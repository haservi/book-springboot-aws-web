spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL57Dialect

  h2:
    console:
      enabled: true
  mustache:
    suffix: .html

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            client-secret: test
            scope: test
